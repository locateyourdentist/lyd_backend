{{!-- <!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>New User Registered</title>
  <style>
    /* Reset some defaults for better email compatibility */
    body, p, h1, h2, h3, table, td {
      margin: 0;
      padding: 0;
      font-family: 'Segoe UI', Tahoma, sans-serif;
    }

    body {
      background-color: #f4f6f8;
      color: #004958;
      line-height: 1.6;
    }

    .email-container {
      width: 100%;
      max-width: 600px;
      margin: 40px auto;
      background-color: #ffffff;
      border-radius: 8px;
      overflow: hidden;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    }

    .email-header {
      background-color: #004958;
      color: #ffffff;
      padding: 30px;
      text-align: center;
    }

    .email-header h2 {
      font-size: 24px;
    }

    .email-content {
      padding: 30px;
    }

    .info-box {
      background-color: #f9fafb;
      border-left: 4px solid #22c55e;
      padding: 20px;
      border-radius: 6px;
      margin-top: 20px;
    }

    .info-box p {
      font-size: 15px;
      margin-bottom: 10px;
    }

    .info-label {
      font-weight: bold;
      color: #1f2937;
    }

    .footer {
      background-color: #f4f6f8;
      color: #6b7280;
      text-align: center;
      font-size: 13px;
      padding: 20px;
      margin-top: 20px;
    }

    /* Responsive for mobile */
    @media only screen and (max-width: 620px) {
      .email-container {
        margin: 20px;
      }
      .email-header h2 {
        font-size: 20px;
      }
    }
  </style>
</head>

<body>
  <div class="email-container">
    <!-- Header -->
    <div class="email-header">
      <h2>🎉 New User Registered!</h2>
    </div>

    <!-- Content -->
    <div class="email-content">
      <p>Hello Admin,</p>
      <p>A new user has just registered in the system. Details are below:</p>

      <div class="info-box">
        <p><span class="info-label">User ID:</span> {{userId}}</p>
        <p><span class="info-label">Name:</span> {{name}}</p>
        <p><span class="info-label">Mobile:</span> {{mobile}}</p>
        <p><span class="info-label">Email:</span> {{email}}</p>
        <p><span class="info-label">Registered On:</span> {{registeredOn}}</p>
      </div>
    </div>

    <!-- Footer -->
    <div class="footer">
      &copy; {{year}} Your Company. All rights reserved.
    </div>
  </div>
</body>
</html> --}}
<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title>New User Registered</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #f1f5f9;
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
      color: #334155;
    }

    .wrapper {
      padding: 40px 20px;
    }

    .email-container {
      width: 100%;
      max-width: 600px;
      margin: auto;
      background-color: #ffffff;
      border-radius: 14px;
      overflow: hidden;
      box-shadow: 0 10px 30px rgba(0,0,0,0.08);
    }

    .email-header {
      background: linear-gradient(135deg, #004958, #006d7f);
      color: #ffffff;
      padding: 35px 30px;
      text-align: center;
    }

    .logo {
      max-width: 110px;
      margin-bottom: 15px;
    }

    .email-header h2 {
      margin: 0;
      font-size: 22px;
      font-weight: 600;
      letter-spacing: 0.5px;
    }

    .email-content {
      padding: 35px 30px;
      font-size: 15px;
      line-height: 1.6;
    }

    .highlight-box {
      background: #f8fafc;
      border: 1px solid #e2e8f0;
      border-radius: 10px;
      padding: 22px;
      margin-top: 25px;
    }

    .info-row {
      margin-bottom: 12px;
    }

    .info-label {
      font-weight: 600;
      color: #0f172a;
      display: inline-block;
      min-width: 120px;
    }

    .footer {
      background-color: #f8fafc;
      color: #94a3b8;
      text-align: center;
      font-size: 12px;
      padding: 20px;
    }

    @media (max-width: 600px) {
      .email-content {
        padding: 25px 20px;
      }

      .email-header {
        padding: 30px 20px;
      }

      .info-label {
        display: block;
        margin-bottom: 4px;
      }
    }
  </style>
</head>

<body>
  <div class="wrapper">
    <div class="email-container">

      <!-- Header -->
      <div class="email-header">
        <!-- App Logo -->
        <img src="{{logoUrl}}" alt="Company Logo" class="logo" />
        <h2>🎉 New User Registered</h2>
      </div>

      <!-- Content -->
      <div class="email-content">
        <p>Hello Admin,</p>
        <p>A new user has successfully registered in the system. Here are the details:</p>

        <div class="highlight-box">
          <div class="info-row">
            <span class="info-label">User ID:</span> {{userId}}
          </div>
          <div class="info-row">
            <span class="info-label">Name:</span> {{name}}
          </div>
          <div class="info-row">
            <span class="info-label">Mobile:</span> {{mobile}}
          </div>
          <div class="info-row">
            <span class="info-label">Email:</span> {{email}}
          </div>
          <div class="info-row">
            <span class="info-label">Registered On:</span> {{registeredOn}}
          </div>
        </div>
      </div>

      <!-- Footer -->
      <div class="footer">
        © {{year}} Your Company. All rights reserved.
      </div>

    </div>
  </div>
</body>
</html>